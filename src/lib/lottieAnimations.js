import fastDelivery from "@/assets/lottie/fast-delivery.json";
import packageAlert from "@/assets/lottie/package-alert.json";

// Success → fast-delivery (truck/package in motion)
export const successAnimation = fastDelivery;

// Occurrence → package with pulsing alert
export const occurrenceAnimation = packageAlert;