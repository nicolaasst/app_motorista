// Mascotes (traço + preenchimento amarelo) para estados vazios/comemoração.
// Origem das imagens: src/lib/brandAssets.js (arquivo local ou URL atual).
import { MASCOTES } from "@/lib/brandAssets";

export const ILLUSTRATIONS = MASCOTES;

export default ILLUSTRATIONS;
